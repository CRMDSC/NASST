import { createTheme, ThemeProvider } from '@mui/material/styles';



export const muiTheme = createTheme({
  palette: {
    primary: {
      main: '#8FC6DC',
    },
    secondary: {
      main: '#E6FAFD',
    },
  },
});
